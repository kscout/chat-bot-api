# HTTP service chart
http:
  secret:
    - key: apikey
      value: '...'
      envKey: API_KEY
    - key: workspaceid
      value: '...'
      envKey: WORKSPACE_ID

# Mongo chart
mongo:
  dbPassword: '...'
